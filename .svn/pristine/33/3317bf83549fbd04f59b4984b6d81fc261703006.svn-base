function hbar=computehbar(xTe,sigma, lambda)
% function [hbar]=computehbar(xTe,sigma, lambda);
% 
% computes the expected prediction of the average classifier (hbar)
% for data set xTe. 
%
% The training data of size Nsmall drawn from toydata.m with OFFSET 
% with kernel regression with sigma and lambda
%
% The "infinite" number of models is estimated as an average over NMODELS. 
%
% INPUT:
% xTe   | 2xn matrix, of n column-wise input vectors (each 2-dimensional)
% sigma | kernel width of the RBF kernel
% lambda | regularization constant
%

global Nsmall NMODELS OFFSET

[~,n]=size(xTe);
hbar=zeros(1,n);

for j=1:NMODELS

end;
hbar=hbar./NMODELS;


